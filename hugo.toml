baseURL = 'https://tistheseasonkc.com/'
locale = 'en-us'
title = 'Tis The Season Holiday Lighting'
enableRobotsTXT = true

[markup.tableOfContents]
  startLevel = 2
  endLevel = 2

[taxonomies]
  # Disable default taxonomies — this site doesn't use tags or categories

[sitemap]
  changefreq = 'monthly'
  priority = 0.5

[outputs]
  home = ['HTML', 'RSS', 'RobotsTXT', 'Markdown']
  page = ['HTML', 'Markdown']
  section = ['HTML', 'Markdown']

[outputFormats.RobotsTXT]
  mediaType = 'text/plain'
  baseName = 'robots'
  isPlainText = true
  notAlternative = true

[outputFormats.Markdown]
  mediaType = 'text/markdown'
  baseName = 'index'
  isPlainText = true
  notAlternative = true

[params]
  description = "Professional holiday lighting installation in Lee's Summit, MO and surrounding areas. Residential and commercial. Free estimates, full-service install, maintenance, takedown & storage."
  phone = '816-281-7032'
  textPhone = '844-600-1675'
  email = 'hello@tts.lighting'
  companyName = 'Tis The Season Holiday Lighting'
  tagline = 'Professional Holiday Lighting'
  primaryCity = "Lee's Summit, MO"
  inBusinessSince = '2015'
  # Default OG image (fallback for all pages)
  ogImage = '/images/og/social.jpg'
  # Google Places API key (restricted to tistheseasonkc.com referrer + Places API only)
  googleMapsApiKey = 'AIzaSyAtwWskZSb5_b3HOIxCAnaG4I8jS4paqaM'
  # Meta Pixel / dataset ID ("TTS Website"). Public by nature; the Conversions
  # API token is server-side only (META_CAPI_ACCESS_TOKEN). Leave empty to
  # omit the Pixel from the build. Per-page opt-out: `tracking.metaPixel: false`.
  metaPixelId = '1737341487496114'

[menus]
  [[menus.main]]
    name = 'Home'
    pageRef = '/'
    weight = 10
  [[menus.main]]
    name = 'Services'
    pageRef = '/services/'
    weight = 20
  [[menus.main]]
    name = 'Areas'
    pageRef = '/service-areas/'
    weight = 30
  [[menus.main]]
    name = 'Gallery'
    pageRef = '/gallery/'
    weight = 40
  [[menus.main]]
    name = 'About'
    pageRef = '/about/'
    weight = 50
  [[menus.main]]
    name = 'Contact'
    pageRef = '/contact/'
    weight = 60

  [[menus.footer]]
    name = 'Holiday Lighting'
    pageRef = '/holiday-lighting/'
    weight = 10
  [[menus.footer]]
    name = 'Gallery'
    pageRef = '/gallery/'
    weight = 20
  [[menus.footer]]
    name = 'FAQ'
    pageRef = '/faq/'
    weight = 30
  [[menus.footer]]
    name = 'Fix Request'
    pageRef = '/create-fix-request/'
    weight = 40
  [[menus.footer]]
    name = 'Terms'
    pageRef = '/terms/'
    weight = 50
